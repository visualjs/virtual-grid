{
  "name": "virtual-grid",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --force",
    "build": "vite build && tsc -p ./tsconfig.json -emitDeclarationOnly",
    "serve": "vite preview",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.5",
    "ts-jest": "^27.0.6",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
